<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Jika permintaan bukan ke folder dist, arahkan ke dist
    RewriteCond %{REQUEST_URI} !^/dist/
    RewriteRule ^(.*)$ /dist/$1 [L,QSA]
</IfModule>
